server:
  port: 8082

spring:
  application:
    name: service-b

management:
  endpoints:
    web:
      exposure:
        include: '*'

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
      service-a:
        connectTimeout: 3000
        readTimeout: 10000
  circuitbreaker:
    enabled: false
